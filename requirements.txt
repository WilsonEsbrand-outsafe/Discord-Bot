# 서버(Oracle Cloud)와 개발 PC의 파이썬 버전이 달라서 정확한 핀(==)을 쓰면
# 한쪽에서 설치가 깨진다. 실제로 matplotlib==3.11.1 을 박았다가 서버 배포가
# 실패했다(서버 파이썬이 더 낮아 3.10.x 까지만 있음). 하한만 지정한다.
discord.py>=2.4.0
python-dotenv>=1.0.1
aiohttp>=3.9.5
pytz>=2024.1
matplotlib>=3.5          # cogs/players_market.py 의 /시세 그래프

# feedparser 는 cogs/transfer_tracker.py 전용. 이 cog 는 Romano 의 X 피드
# 확보 방안이 정해질 때까지 bot.py 의 EXTENSIONS 에서 빠져 있다.
# 다시 켤 때 아래 주석을 해제할 것.
# feedparser>=6.0.11
